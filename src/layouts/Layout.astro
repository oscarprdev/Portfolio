---
import Footer from '../components/common/Footer.astro';
import { getLangFromUrl } from '../i18n/utils';

interface Props {
    title: string;
}

const { title } = Astro.props;
const lang = getLangFromUrl(Astro.url);
---

<!doctype html>
<html lang={lang}>
    <head>
        <meta charset="UTF-8" />
        <meta
            name="description"
            content="Portfolio desarrollo web de Oscar Perez Romero - @oscarprdev"
        />
        <meta
            name="viewport"
            content="width=device-width"
        />
        <link
            rel="icon"
            type="image/svg+xml"
            href="/favicon.svg"
        />
        <title>{title}</title>
        <meta
            name="generator"
            content={Astro.generator}
        />
        <title>{title}</title>
    </head>
    <body>
        <slot />
        <Footer />
    </body>
</html>
<style is:global>
    :root {
        /* Fonts Sizes*/
        --title-size: 5rem;

        /* Colors */
        --red-light: #ff1e0b;

        --text-secondary: rgb(181, 181, 181);

        /* --default-color: #333; */
        --default-color: #dedede;
        --default-color-light: #b5b5b5;

        --badge-dark: #2d3299;
        --badge-light: #773e9d;

        --badge-container-dark: #6982ff;
        --badge-container-light: #8e55ff;

        --bg-glow: #6982ff1a;
        --bg-glow-dark: #8e55ff10;
        /* Backgrounds */
        /* --bg-screen: #ededed; */
        --bg-screen: #13151a;
        --bg-screen-light: #646464a7;

        /* Shadow */
        --box-shadow: rgba(0, 0, 0, 0.24) 0px 3px 8px;
    }

    @font-face {
        font-family: 'HelveticaNeue';
        src: url('/fonts/HelveticaNeue.otf') format('opentype');
        font-display: swap;
    }

    html {
        background: #13151a;
        background-size: 224px;
        scroll-behavior: smooth;
        overflow-x: hidden;
    }

    * {
        box-sizing: border-box;
    }

    body {
        font-family: Arial, Helvetica, sans-serif, system-ui;

        color: var(--default-color);
        background-color: var(--bg-screen);

        margin: 0;
        width: 100vw;
    }

    a {
        color: inherit;
        text-decoration: none;
    }

    h1 {
        font-family: Arial, Helvetica, sans-serif, system-ui;
    }

    ul {
        list-style: none;
    }

    li {
        text-indent: 0;
        list-style-type: 0;
    }

    h2 {
        font-family: system-ui;
    }

    picture {
        overflow: hidden;
    }

    img {
        object-fit: cover;
        height: 100%;
        width: 100%;
    }

    .shared-icon {
        display: grid;
        place-items: center;
        z-index: 10;
    }

    .bg-noise {
        background-position: 50%;
        background-repeat: repeat;
        background-image: url(/assets/noise.png);
        background-size: 140px 100px;
    }

    .section-title {
        background: radial-gradient(50% 75% at 10% 10%, #f1f1f192 0, #ffffff);
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .color-text {
        background: linear-gradient(
            to right,
            var(--badge-container-dark) 15%,
            var(--badge-container-light) 45%
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .border {
        z-index: 10;
        position: absolute;
        top: 0%;
        bottom: auto;
        left: 0%;
        right: 0%;
        width: 100%;
        height: 1px;
    }

    .border--light {
        background-image: linear-gradient(
            to right,
            rgba(0, 0, 0, 0) 5%,
            rgba(255, 255, 255, 1) 35%,
            #fff 50%,
            rgba(255, 255, 255, 1) 65%,
            rgba(0, 0, 0, 0) 95%
        );
    }

    .border--dark {
        background-image: linear-gradient(
            to right,
            rgba(0, 0, 0, 0) 5%,
            rgb(192, 192, 192) 35%,
            #dcdcdc 50%,
            rgb(192, 192, 192) 65%,
            rgba(0, 0, 0, 0) 95%
        );
    }

    .show {
        view-timeline-name: --show;
        view-timeline-axis: block;

        animation-timeline: --show;
        animation-name: show;
        animation-range: entry 18% cover 50%;
        animation-fill-mode: both;
    }

    @keyframes show {
        from {
            opacity: 0;
            transform: translateY(55%);
        }

        to {
            opacity: 1;
            transform: translateY(0%);
        }
    }
</style>
